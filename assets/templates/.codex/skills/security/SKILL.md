---
name: security
description: Security for risk-tagged features and the project baseline: a threat pass at spec (data classification, denied actions, enforcing rules, input surfaces including model prompts, abuse cases), a diff checklist at QA, and a baseline at tech setup. Use when the owner says "security", "is this safe", "who can see this", "review permissions", or when a feature tagged money, privacy, sign-in or external service reaches spec or QA.
---

Follow the playbook at `docs/playbooks/security.md` exactly. It is the single source of truth for this
command; this file only makes it invokable. Read `AGENTS.md` first if you have not this session.
