# Regression: golden paths

One row per shipped feature: the shortest sequence of steps that proves it still works. Appended
at Close, walked at every QA run. If a row stops passing, that is a blocker for whatever change
broke it, however unrelated it looks.

| Feature | Golden path | Expected | Added | Last passed |
|---|---|---|---|---|
| | | | | |
