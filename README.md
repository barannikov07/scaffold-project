# Old site address, redirected

This branch only keeps the old walkthrough address alive. Both pages redirect to
https://barannikov07.github.io/scaffold-project-site/, where the site now lives, together with
the example project. The skill itself is on `main`.
