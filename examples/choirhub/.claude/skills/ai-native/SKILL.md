---
name: ai-native
description: Find where AI inside the product removes a step from the user's flow: read the PRD, propose two to four concrete applied-AI opportunities (voice to record, paste to record, auto-mapping, drafted messages, drift signals, confirmation-card agents) each with the step removed, the gain, the risk and a recommendation, and carry chosen ones into design, spec and QA. Use after every PRD draft and whenever the owner says "where can AI help", "make it smarter", or "AI features".
---

Follow the playbook at `docs/playbooks/ai-native.md` exactly. It is the single source of truth for this
command; this file only makes it invokable. Read `AGENTS.md` first if you have not this session.
